[gpg]
    args = ["--quiet"]

[git]
    autocommit = true

[edit]
    command = "nvim"

[merge]
    command = "nvim"
    args = ["-d", "{{`{{ .Destination }}`}}", "{{`{{ .Source }}", "{{ .Target }}`}}"]

[diff]
    pager = "delta --side-by-side"

